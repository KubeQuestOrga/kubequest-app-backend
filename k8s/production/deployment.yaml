apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-project-kubequest-backend-deployment
  namespace: prod-project-kubequest-backend
  labels:
    app: prod-project-kubequest-backend
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-project-kubequest-backend
  template:
    metadata:
      labels:
        app: prod-project-kubequest-backend
    spec:
      containers:
        - name: prod-project-kubequest-backend
          image: corentin35/project-kubequest-backend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
