apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-project-kubequest-backend-ingress
  namespace: staging-project-kubequest-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.kubequest.api.flapi.org
      secretName: staging-project-kubequest-backend-tls
  rules:
    - host: staging.kubequest.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-project-kubequest-backend-service
                port:
                  number: 3333
